import { EmployeForm } from "@/components/forms";

export default function Cadastro(){
    return (
        <div className="container flex flex-col items-center mt-4 rounded-lg min-h-screen bg-cyan-600">
            <EmployeForm />
        </div>
    );
}